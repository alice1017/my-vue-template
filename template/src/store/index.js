import Vue from "vue";
import Vuex from "vuex";
import * as types from "./types";

Vue.use(Vuex);

const state = {

};
const getters = {

};
const mutations = {
    [types.MUTATION_TYPE]: state => {}
};
const actions = {

};

export default new Vuex.Store({
    state,
    getters,
    mutations,
    actions
});
